server:
  port: 8080

spring:

#  datasource:
#    url: jdbc:h2:mem:testdb
  sql:
    init:
      mode: never
#      data-locations: data-test.sql
#      schema-locations: schema-test.sql

  flyway:
#    url: jdbc:postgresql://localhost:5433/library-test
    user: postgres
    password: postgres
    enabled: true
    locations: ['classpath:db/migration/postgres', 'classpath:test/resources/db/migration/postgres']

  r2dbc:
#    url: r2dbc:postgresql://localhost:5433/library-test
    username: postgres
    password: postgres